# 开发环境配置
spring:
  # 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/meeting_booking?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

# JWT配置
jwt:
  secret: meeting_room_booking_secret_key_2024
  expiration: 86400000  # 24小时，单位毫秒